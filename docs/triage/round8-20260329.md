# Triage — Round 8 (Config Wiring Reviews)

**Date:** 2026-03-29
**Reviews aggregated:**
- `docs/reviews/config-wiring-20260329-175000-claude.md`
- `docs/reviews/config-wiring-20260329-181454-codex.md`
- `docs/reviews/config-wiring-20260329-181403-gemini.md`

**Branch:** `feature/config-extraction`
**State:** 83 tests, 213 assertions, both gates green (bun test + bun run typecheck)

---

## Cross-family consensus

| Finding | Families | Agreed severity |
|---------|----------|-----------------|
| Duplicate defaults — Zod schema and provider destructuring | Claude HIGH + Codex HIGH + Gemini MEDIUM | **HIGH** |
| Package boundary doesn't export config | Codex MEDIUM | **MEDIUM** |
| Expiry buffer test proves direction, not magnitude | Claude MEDIUM | **LOW** |
| 3 retry config values have no explicit wiring test | Claude MEDIUM | **LOW** |
| URL resolved on every call, not once in constructor | Claude MEDIUM | **MEDIUM** |
| Duplicate RetryConfig/UrlConfig types | Claude LOW + Gemini LOW | **LOW** |

---

## Coder is doing now

### Fix 1: Resolve config once in constructor

**Source:** Claude HIGH + Codex HIGH + Gemini MEDIUM (3-family consensus)
**Safe to apply now** — internal refactor, no behavior change

The `??` fallbacks are scattered through `getRates()` and `acquireToken()`. Move
resolution to the constructor: store `ratingUrl`, `tokenUrl`, retry fields, and
`tokenExpiryBufferSeconds` as private readonly fields. Methods read from resolved
fields. Defaults exist in one place (constructor), not duplicated in every method.

This also addresses Claude MEDIUM (URL resolved on every call).

### Fix 2: Export from index.ts

**Source:** Codex MEDIUM
**Safe to apply now** — no behavior change

`loadUpsConfig`, `UpsConfig`, `UpsRateProvider`, and `FetchFn` should be
accessible through the package boundary.

---

## Deferred (no action now)

| # | Finding | Deferred to | Source |
|---|---------|-------------|--------|
| Make retry/urls required, eliminate constructor defaults | Step 11 (carrier interface extraction) | Claude HIGH + Codex HIGH |
| Factory function bridging loadUpsConfig → provider | Step 11 | Claude HIGH |
| Integration test: loadUpsConfig output → provider | Step 11 | Claude HIGH |
| Expiry buffer test precision (direction vs magnitude) | Tester | Claude MEDIUM |
| Wiring tests for baseDelayMs/timeoutMs/maxRetryAfterSeconds | Tester | Claude MEDIUM |
| Duplicate RetryConfig/UrlConfig types | Janitor pass | Claude LOW + Gemini LOW |
| Test helper/factory for provider construction | Janitor pass | Gemini LOW |
| Carried: 401 reacquire-and-retry | Design decision (D002) | Codex HIGH (round 6) |
| Carried: Mock delay injection | Janitor pass (D012) | Claude MED (round 6) |
